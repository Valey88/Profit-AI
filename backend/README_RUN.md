# 🚀 Инструкция по запуску Backend (Python/FastAPI)

Эта инструкция поможет вам запустить серверную часть проекта (Backend) с полного нуля на операционных системах **Windows** и **Linux**.

---

## 🛠️ 1. Подготовка окружения (Prerequisites)

Перед началом убедитесь, что у вас установлен **Python**.

### Для Windows:
1.  Скачайте Python 3.11+ с официального сайта: [python.org/downloads](https://www.python.org/downloads/).
2.  При установке **ОБЯЗАТЕЛЬНО** поставьте галочку **"Add Python to PATH"** (Добавить Python в PATH).

### Для Linux (Ubuntu/Debian):
Откройте терминал и выполните:
```bash
sudo apt update
sudo apt install python3 python3-pip python3-venv git -y
```

---

## 📦 2. Установка проекта

### Шаг 2.1: Открытие терминала

-   **Windows**: Откройте командную строку (`cmd`) или PowerShell.
-   **Linux**: Откройте стандартный терминал.

### Шаг 2.2: Переход в папку backend
Перейдите в директорию `backend`, где лежит этот файл.
*Пример (замените путь на свой):*
```bash
cd backend
```

---

## 🐍 3. Виртуальное окружение (Virtual Environment)
Рекомендуется использовать виртуальное окружение, чтобы не засорять систему.

### Windows:
Создание:
```powershell
python -m venv venv
```
Активация:
```powershell
.\venv\Scripts\activate
```
*(Если увидите ошибку о правах доступа, выполните `Set-ExecutionPolicy RemoteSigned` в PowerShell от имени администратора, или используйте `cmd`: `venv\Scripts\activate.bat`)*

### Linux:
Создание:
```bash
python3 -m venv venv
```
Активация:
```bash
source venv/bin/activate
```

После активации в начале строки терминала появится надпись `(venv)`.

---

## 📥 4. Установка зависимостей

Установите все необходимые библиотеки одной командой:
```bash
pip install -r requirements.txt
```

---

## ⚙️ 5. Настройка конфигурации (.env)

Создайте файл с именем `.env` в папке `backend`.
Для Windows можно использовать блокнот: `notepad .env`
Для Linux: `nano .env`

Вставьте туда следующее содержимое (минимальные настройки для локального запуска):

```ini
# --- Основные настройки ---
PROJECT_NAME="Profit Flow AI"
# Секретный ключ (для локального теста можно оставить этот)
SECRET_KEY="dev-secret-key-change-me"

# --- База данных ---
# По умолчанию используется SQLite (файл data.db создастся автоматически)
USE_SQLITE=True
# Если хотите Postgres, установите USE_SQLITE=False и заполните данные ниже:
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=password
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_DB=profitflow

# --- Redis (опционально для кэша/сокетов) ---
# Если Redis не запущен, приложение может работать, но функции реального времени (сокеты) могут быть ограничены.
REDIS_HOST=localhost
REDIS_PORT=6379

# --- AI (OpenAI / OpenRouter) ---
# Вставьте сюда свой ключ, если нужна работа с AI
OPENAI_API_KEY=""
# Для OpenRouter используйте: https://openrouter.ai/api/v1
# Для оригинального OpenAI: https://api.openai.com/v1
OPENAI_BASE_URL="https://api.openai.com/v1"
OPENAI_MODEL="gpt-4-turbo"
```

Сохраните файл.

---

## 🗄️ 6. База данных (Миграции)

Инициализируйте базу данных (создаст таблицы).
Выполните команду:

```bash
alembic upgrade head
```

*Если возникнет ошибка "alembic не найден", убедитесь, что виртуальное окружение активно, или попробуйте `python -m alembic upgrade head`.*

---

## ▶️ 7. Запуск сервера

Запустите сервер с помощью Uvicorn:

```bash
uvicorn src.main:app --reload
```

-   `src.main:app` — путь к приложению.
-   `--reload` — сервер будет автоматически перезагружаться при изменении кода (удобно для разработки).

Если всё прошло успешно, вы увидите что-то вроде:
```
INFO:     Uvicorn running on http://127.0.0.1:8000 (Press CTRL+C to quit)
```

Откройте браузер и перейдите по адресу:
-   **Документация API**: [http://127.0.0.1:8000/docs](http://127.0.0.1:8000/docs)
-   **Главная**: [http://127.0.0.1:8000/](http://127.0.0.1:8000/)

---

## ❓ Решение проблем

1.  **"python" не является внутренней или внешней командой**:
    -   На Windows: Вы не добавили Python в PATH при установке. Переустановите Python и поставьте галочку "Add to PATH".
    -   На Linux: Попробуйте использовать `python3` вместо `python`.

2.  **Ошибки при `pip install`**:
    -   Убедитесь, что интернет работает.
    -   Попробуйте обновить pip: `python -m pip install --upgrade pip`.
    -   На Windows иногда требуются C++ Build Tools (обычно для библиотек вроде `asyncpg` или `uvloop`, но в текущем списке зависимостей чистый Python/binary wheels должны работать).

3.  **Порт 8000 занят**:
    -   Запустите на другом порту:
        ```bash
        uvicorn src.main:app --reload --port 8001
        ```
